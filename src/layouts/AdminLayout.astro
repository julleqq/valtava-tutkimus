---
interface Props {
  title: string;
  active?: 'postaukset' | 'aiheet' | 'kirjoittajat';
}
const { title, active } = Astro.props;
---

<!doctype html>
<html lang="fi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{title} — Hallinta</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,300;1,300;1,400&family=Inter:wght@300;400&display=swap" rel="stylesheet" />
    <style>
      *, *::before, *::after { box-sizing: border-box; margin: 0; padding: 0; }
      html { background: #0C0C0B; color: #E8E5DE; font-family: 'Inter', system-ui, sans-serif; font-size: 14px; -webkit-font-smoothing: antialiased; }
      body { min-height: 100vh; display: flex; flex-direction: column; }

      /* Topbar */
      .topbar { display: flex; align-items: center; justify-content: space-between; padding: 1.25rem 2rem; border-bottom: 1px solid #252420; flex-shrink: 0; }
      .topbar-left { display: flex; align-items: center; gap: 2rem; }
      .topbar-home { font-family: 'Cormorant Garamond', serif; font-style: italic; font-weight: 300; font-size: 1rem; color: #6B6860; text-decoration: none; }
      .topbar-home:hover { color: #E8E5DE; }
      .topbar-nav { display: flex; align-items: center; gap: 1.5rem; }
      .nav-link { font-size: 0.65rem; letter-spacing: 0.12em; text-transform: uppercase; color: #6B6860; text-decoration: none; padding-bottom: 1px; border-bottom: 1px solid transparent; }
      .nav-link:hover { color: #E8E5DE; }
      .nav-link.active { color: #E8E5DE; border-bottom-color: #E8E5DE; }
      .logout-form { display: inline; }
      .logout-btn { font-family: 'Inter', system-ui, sans-serif; font-size: 0.65rem; letter-spacing: 0.12em; text-transform: uppercase; color: #6B6860; background: none; border: none; cursor: pointer; }
      .logout-btn:hover { color: #E8E5DE; }

      /* Main */
      main { flex: 1; max-width: 720px; width: 100%; margin: 0 auto; padding: 3rem 2rem 6rem; }

      /* Shared form styles — available to all child pages */
      .section-label { font-size: 0.65rem; letter-spacing: 0.15em; text-transform: uppercase; color: #6B6860; margin-bottom: 1.5rem; }
      .section-label a { color: inherit; text-decoration: none; }
      .section-label a:hover { color: #E8E5DE; }
      .divider { border: none; border-top: 1px solid #252420; margin: 2.5rem 0; }

      .flash { font-size: 0.75rem; padding: 0.75rem 1rem; margin-bottom: 2rem; border: 1px solid; }
      .flash-ok  { border-color: #252420; color: #6B6860; }
      .flash-err { border-color: #3a2020; color: #8a6060; }

      .form { display: flex; flex-direction: column; gap: 1.25rem; }
      label { display: block; font-size: 0.65rem; letter-spacing: 0.15em; text-transform: uppercase; color: #6B6860; margin-bottom: 0.4rem; }
      input, select, textarea {
        display: block; width: 100%;
        background: #141413; border: 1px solid #252420;
        color: #E8E5DE; font-family: 'Inter', system-ui, sans-serif;
        font-size: 0.875rem; font-weight: 300; padding: 0.75rem 1rem;
        outline: none; appearance: none; -webkit-appearance: none;
      }
      input:focus, select:focus, textarea:focus { border-color: #6B6860; }
      textarea { resize: vertical; line-height: 1.7; min-height: 280px; }
      .row { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; }
      .btn { font-family: 'Inter', system-ui, sans-serif; font-size: 0.65rem; letter-spacing: 0.15em; text-transform: uppercase; color: #6B6860; background: none; border: none; cursor: pointer; padding: 0; }
      .btn:hover { color: #E8E5DE; }
      .btn-danger:hover { color: #c07070; }

      /* Lists */
      .item-list { display: flex; flex-direction: column; }
      .item-row { display: flex; align-items: baseline; justify-content: space-between; gap: 2rem; padding: 0.9rem 0; border-bottom: 1px solid #252420; }
      .item-row:first-child { border-top: 1px solid #252420; }
      .item-title { font-family: 'Cormorant Garamond', serif; font-style: italic; font-weight: 300; font-size: 1.125rem; color: #E8E5DE; text-decoration: none; }
      .item-title:hover { opacity: 0.6; }
      .item-actions { display: flex; align-items: center; gap: 1.25rem; flex-shrink: 0; }
      .item-meta { font-size: 0.7rem; color: #6B6860; }
      .item-link { font-size: 0.65rem; letter-spacing: 0.1em; text-transform: uppercase; color: #6B6860; text-decoration: none; }
      .item-link:hover, .item-link.active { color: #E8E5DE; }

      /* Collapsible */
      details.collapsible { border-bottom: 1px solid #252420; }
      details.collapsible summary { display: flex; align-items: center; justify-content: space-between; padding: 0.9rem 0; cursor: pointer; list-style: none; }
      details.collapsible summary::-webkit-details-marker { display: none; }
      details.collapsible summary .sum-label { font-size: 0.65rem; letter-spacing: 0.15em; text-transform: uppercase; color: #6B6860; }
      details.collapsible summary .sum-arrow { font-size: 0.65rem; color: #6B6860; transition: transform 0.15s; }
      details.collapsible[open] summary .sum-arrow { transform: rotate(90deg); }
      details.collapsible summary:hover .sum-label,
      details.collapsible summary:hover .sum-arrow { color: #E8E5DE; }
      details.collapsible .inner { padding: 1.25rem 0 1.5rem; display: flex; flex-direction: column; gap: 1rem; }

      #new-topic-row { display: none; margin-top: 0.5rem; }
    </style>
  </head>
  <body>
    <header class="topbar">
      <div class="topbar-left">
        <a href="/admin" class="topbar-home">Valtava Tutkimus</a>
        <nav class="topbar-nav">
          <a href="/admin/postaukset" class={`nav-link${active === 'postaukset' ? ' active' : ''}`}>Postauksia</a>
          <a href="/admin/aiheet" class={`nav-link${active === 'aiheet' ? ' active' : ''}`}>Aiheita</a>
          <a href="/admin/kirjoittajat" class={`nav-link${active === 'kirjoittajat' ? ' active' : ''}`}>Kirjoittajia</a>
        </nav>
      </div>
      <form method="POST" action="/api/logout" class="logout-form">
        <button type="submit" class="logout-btn">Kirjaudu ulos</button>
      </form>
    </header>

    <main>
      <slot />
    </main>
  </body>
</html>
